// class Tree:
//     var left     #default both constructors set to nil, no need to write init in serpent
//     var right

def make_tree(depth):
    if depth == 0:
        return Tree()

// def make_tree(depth):
//     if depth == 0:
//         return Tree()
    // depth = depth - 1

    // var new_tree = Tree()
    // // TODO: assignment to left vals with suffix
    // // new_tree.left = make_tree(depth)
    // // new_tree.right = make_tree(depth)
    
    // return new_tree
