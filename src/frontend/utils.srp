// Hack: uses class initialization to bind a variable to a scope.
class CurryTwo:

    var func
    var fst

    def init(func, fst):
        this.func = func
        this.fst = fst

    def build(snd):
        return funcall(func, fst, snd)
